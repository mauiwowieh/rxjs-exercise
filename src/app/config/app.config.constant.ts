/**
 * Application configuration constants
 */
export const APP_CONFIG = {
  /** Number of selection boxes in the grid */
  BOX_COUNT: 10,

  /** Prefix for localStorage keys */
  STORAGE_PREFIX: 'rxjs-selection',
} as const;
